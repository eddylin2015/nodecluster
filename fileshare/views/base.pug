doctype html
html(lang="en")
  head
    title StudInfo system
    meta(charset='utf-8')
    meta(name="viewport", content="width=device-width, initial-scale=1")
    link(rel="stylesheet", href="//maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap.min.css")

  body
    .navbar.navbar-default
      .container
        .navbar-header
          .navbar-brand 資訊系統

        ul.nav.navbar-nav
          li
            a(href="/") 
              span(class="glyphicon") &#xe021
          li
            a(href="/ls") FileList
          li
            a(href="/file_upload_parser.php") FileUpload
            
        p.navbar-text.navbar-right
          // [START profile]
          if profile
            if profile.image
              img.img-circle(src=profile.image, width=24)
            span #{profile.displayName} &nbsp;
              //a(href="#") (chgusr)
          else
            span
            a(href="/login") Login
          // [END profile]

    .container
      block content
